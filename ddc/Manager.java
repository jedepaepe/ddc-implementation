package ddc;

/**
 * couche/Layer entité/entity (aussi appelé "domain", "business")
 * Modèle pour le Manager
 */
class Manager extends Employe{
	// le manager n'a pas d'attributs spécifiques
	// cette classe sert uniquement à différencier un manager d'un employé
	// notez qu'elle pourrait avoir en attribut la liste des employés qui sont sous ses ordres
	// mais cela n'est pas une obligation et cela rendrait le code plus compliqué
}
